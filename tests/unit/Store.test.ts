import test from "ava";
test("create store", (t) => {
    t.pass();
});

test("insert into store", (t) => {
    t.pass();
    // t.deepEqual(store.("abc"), 2);
    // store.set("abc", 3);
    // t.notDeepEqual(store.get("abc"), 2);
});
